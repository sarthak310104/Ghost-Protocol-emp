# --- Web / API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# --- OTLP ingestion ---
# Parsed via plain pydantic models against the OTLP/HTTP+JSON export shape
# (app/ingestion/otlp_schemas.py) -- no protobuf/gRPC dependency needed.

# --- Database ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.3

# --- Queue ---
celery[redis]==5.4.0
redis==5.0.8

# --- HTTP client (used by an optional integration module) ---
httpx==0.27.2

# --- Utilities ---
numpy==1.26.4
python-multipart==0.0.9
tenacity==9.0.0
structlog==24.4.0
cryptography==43.0.1
